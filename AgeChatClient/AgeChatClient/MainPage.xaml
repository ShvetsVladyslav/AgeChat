<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AgeChatClient.MainPage">

    <StackLayout>
        <StackLayout Orientation="Horizontal" Padding="15" HorizontalOptions="End">
            <Label Text="" x:Name="labelUsername" FontSize="20" VerticalTextAlignment="Center"/>
            <Button Text="Sign out" FontSize="15" BackgroundColor="LightGreen" Clicked ="ButtonLogout_Clicked"></Button>
        </StackLayout>
        <ListView ItemsSource="{Binding userCollection}" SeparatorVisibility="Default" SeparatorColor="Black" RowHeight="70" ItemTapped="ChatLabel_Tapped">
            <ListView.Header>
                <Label Text="Choose chat" Padding="12, 0, 0, 0" FontSize="18"></Label>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout HorizontalOptions="FillAndExpand"  BackgroundColor="{Binding color}">
                            <Label Text="{Binding username}" TextColor="Black" FontSize="25" VerticalOptions="CenterAndExpand" Padding="12"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
